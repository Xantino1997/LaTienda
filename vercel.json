{
  "version": 2,
  "builds": [
    {
      "src": "LaTienda/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "lATIENDA/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/LaTienda/$1"
    }
  ],
  "framework": "create-react-app",
  "buildCommand": "cd LaTienda && npm install && npm run build",
  "outputDirectory": "LaTienda/build"
}
